{
  "name": "react-src",
  "version": "0.1.0",
  "scripts": {
    "start": "react-scripts start",
    "postinstall": "npm install --prefix server && npm install --prefix frontend-app"
  },
  "engines": {
    "node": "9.8.0",
    "npm": "5.6.0"
  }
}